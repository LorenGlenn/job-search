<!DOCTYPE html>
<html>
  <head>
    <title>Job Search</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
      <nav class = "nav navbar navbar-inverse shift">
        Job Search |
        <%= link_to "Search", searches_path %>
          <div class="pull-right shift">

          <% if current_user %>
            <%= current_user.email %></li>
             <span class="glyphicon glyphicon-align-justify" aria-hidden="true"></span> <%= link_to "Log-out", destroy_user_session_path, method: "delete" %>
          <% else %>
             <span class="glyphicon glyphicon-off" aria-hidden="true"></span>
            <%= link_to "Log-in", new_user_session_path %></a> |
             <%= link_to "Signup", new_user_registration_path %></a>
          <% end %>
        </div>
      </nav>
        <div class="container">
          <div class="alert-warning">
          <%= flash[:alert] %>
          <%= flash[:notice] %>
        </div>
          <%= yield %>
      </div>
  </body>
</html>
